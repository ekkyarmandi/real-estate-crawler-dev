listing_insert_query = """
INSERT INTO listings_listing (
    id,
    created_at,
    updated_at,
    first_seen_at,
    last_seen_at,
    source_id,
    seller_id,
    url,
    title,
    short_description,
    detail_description,
    price,
    price_currency,
    status,
    valid_from,
    valid_to,
    total_views,
    city,
    municipality,
    micro_location,
    latitude,
    longitude
) VALUES (
    :listing_id,
    now(),
    now(),
    now(),
    now(),
    :source_id,
    :seller_id,
    :url,
    :title,
    :short_description,
    :detail_description,
    :price,
    :price_currency,
    :status,
    :valid_from,
    :valid_to,
    :total_views,
    :city,
    :municipality,
    :micro_location,
    :latitude,
    :longitude
) ON CONFLICT (url) DO UPDATE SET last_seen_at = now(), seller_id = :seller_id, source_id = :source_id;
"""
